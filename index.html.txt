<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>나의 p5.js 포트폴리오</title>
    <style>
        /* 심미성을 극대화한 세련된 남색(Navy/Midnight Blue) 테마 */
        body {
            font-family: 'Pretendard', -apple-system, sans-serif;
            background-color: #0a1128; /* 깊은 남색 배경 */
            color: #e2e8f0; /* 부드러운 밝은 회색 글씨 */
            margin: 0;
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        header {
            text-align: center;
            margin-bottom: 45px;
        }

        h1 {
            color: #38bdf8; /* 화사한 스카이 블루 포인트 컬러 */
            font-size: 2.6rem;
            margin-bottom: 12px;
            font-weight: 700;
            letter-spacing: -0.5px;
        }

        p {
            color: #94a3b8; /* 차분한 톤의 폰트 컬러 */
            font-size: 1.1rem;
            margin: 0;
        }

        /* 요구사항: 2행 2열 그리드 구조 */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 32px;
            max-width: 1200px;
            width: 100%;
        }

        /* 카드 디자인 감각적인 남색 레이어로 변경 */
        .card {
            background-color: #111a36; /* 밝은 남색 카드 배경 */
            border: 1px solid #1e295d; /* 세련된 네이비 테두리 */
            border-radius: 16px; /* 부드러운 라운딩 처리 */
            padding: 24px;
            box-shadow: 0 10px 25px -5px rgba(2, 6, 23, 0.5); /* 은은한 그림자 */
            display: flex;
            flex-direction: column;
            transition: transform 0.2s ease, border-color 0.2s ease;
        }

        /* 마우스를 올렸을 때 살짝 뜨는 효과로 심미성 추가 */
        .card:hover {
            transform: translateY(-4px);
            border-color: #38bdf8;
        }

        .card h3 {
            margin-top: 0;
            margin-bottom: 16px;
            color: #ffffff;
            font-size: 1.3rem;
            border-bottom: 2px solid #1e295d;
            padding-bottom: 12px;
        }

        /* 과제물이 꽉 차고 깔끔하게 보이도록 설정 */
        iframe {
            width: 100%;
            height: 400px;
            border: none;
            border-radius: 10px;
            background-color: #ffffff; /* p5.js 캔버스 가시성을 위해 내부는 흰색 유지 */
        }
        
        .desc {
            font-size: 0.95rem;
            color: #94a3b8;
            margin-top: 16px;
            margin-bottom: 0;
            line-height: 1.5;
        }
    </style>
</head>
<body>

    <header>
        <h1>p5.js를 이용한 여러가지 작품</h1>
        <p>아래 링크를 클릭하여 여러가지 작품을 구경해보세요!</p>
    </header>

    <!-- 요구사항 배정 규칙 준수: 과제1|과제4, 과제2|과제3 -->
    <div class="grid-container">
        
        <!-- 1행 1열: 과제 1 -->
        <div class="card">
            <h3>과제 1 : 정적 작품</h3>
            <iframe src="assignment1/index.html"></iframe>
            <p class="desc">나만의 초상화 그리기 입니다.</p>
        </div>

        <!-- 1행 2열: 과제 4 -->
        <div class="card">
            <h3>과제 4 : 동적 작품</h3>
            <iframe src="assignment4/index.html"></iframe>
            <p class="desc">나의 캐릭커쳐 그리기 입니다.</p>
        </div>

        <!-- 2행 1열: 과제 2 -->
        <div class="card">
            <h3>과제 2 : 정적 작품</h3>
            <iframe src="assignment2/index.html"></iframe>
            <p class="desc">과제2번에서 진행한 캐릭커쳐에 움직임을 넣어 그렸습니다.</p>
        </div>

        <!-- 2행 2열: 과제 3 -->
        <div class="card">
            <h3>과제 3 : 동적 작품</h3>
            <iframe src="assignment3/index.html"></iframe>
            <p class="desc">과제2번에서 진행한 초상화에 다양한 변화를 주었습니다.</p>
        </div>

    </div>

</body>
</html>